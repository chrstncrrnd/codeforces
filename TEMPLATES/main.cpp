#include <iostream>

using namespace std;

#define intinput(var) { \
  string _buf; \
  getline(cin, _buf); \
  (var) = stoi(_buf); \
}

#define strinput(var) {\
  getline(cin, (var)); \
}


int main(void){

  return 0;
}

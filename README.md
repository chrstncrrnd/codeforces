# Codeforces

Some solutions to [codeforces](https://codeforces.com/) problems.

The directory `TEMPLATES` includes base boilerplate files for solutions in 
different programming languages.

The script `new.sh` automatically creates directories and template files 
for the given problem.

